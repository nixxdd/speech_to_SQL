NOTA: For all commands must be in a terminal inside the project folder

--- Start container ---
docker compose up -d

--- Stop container ---
docker compose down -v

--- Enter postgre container ---
docker exec -it sts-db psql -U db_manager -d wrenai

--- Start FastAPI Backend ---
fastapi dev

--- Start StreamLit Frontend ---
streamlit run interface.py